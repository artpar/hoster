version: "3.8"

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost"]
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 5s
